# Advent-of-code Solution from Jonas

I use <code>
input.splice(\`<br />
\`)</code>
instead of
<code>input.splice('\n')</code>
to avoid formatting issues with unix `\n` or windows `\r\n` new line rules.
